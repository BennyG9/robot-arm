function Q = path2angles(path, robot)
    Q = [];
    for pos = path

        q = inverse_kinematics(pos, robot);
        
        Q(1:3,end+1) = q(1:3,1);

        if(length(Q) == 1)
            continue;
        end
        for qi = q
            
        end
    end
end